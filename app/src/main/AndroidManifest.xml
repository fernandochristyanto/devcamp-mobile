<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.devcamp.devcamp_mobile">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:allowBackup="true"
            android:label="@string/app_name"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            android:usesCleartextTraffic="true">
        <activity android:name=".module.Register.RegisterTokoViewModel">
        </activity>
        <activity android:name=".module.garage_sale.GarageSaleActivity">
        </activity>
        <activity android:name=".module.login.LoginActivity">
        </activity>
        <activity android:name=".module.main.MainActivity">
        </activity>
        <activity android:name=".module.register.RegisterTokoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <action android:name="android.intent.action.VIEW"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>